---
phase: 01-project-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
  - backend/requirements.txt
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/index.html
autonomous: true
user_setup: []
must_haves:
  truths:
    - Repository directory structure contains backend, frontend, ml, hardware, and docs
    - Python dependencies specified with exact compatibility in backend/requirements.txt
    - Frontend build manifests specified with Vite, React, and Tailwind CSS in frontend/
  artifacts:
    - path: backend/requirements.txt
      description: Backend Python dependencies
    - path: frontend/package.json
      description: Frontend Node.js dependencies
  key_links:
    - from: backend/requirements.txt
      to: ARCH-02
    - from: frontend/package.json
      to: ARCH-02
---

# Phase 1 Plan 01: Repository Scaffolding and Dependency Configuration

## Objective

- **What:** Initialize the modular directory structure (`backend/`, `frontend/`, `ml/`, `hardware/`, `docs/`), root configuration files (`.gitignore`, `README.md`), backend dependencies (`backend/requirements.txt`), and frontend build configuration (`frontend/package.json`, `vite.config.ts`, `tsconfig.json`, `tailwind.config.js`, `postcss.config.js`).
- **Why:** Establishes a clean, reproducible baseline for both backend Python services and frontend React applications, satisfying ARCH-01 and ARCH-02 before implementing domain logic.
- **Output:** Scaffolding directories, root documentation, backend requirements specification, and frontend configuration manifests.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

## Tasks

<task type="code">
  <name>Task 1: Repository Directory Structure & Root Files (ARCH-01)</name>
  <files>.gitignore, README.md, backend/.gitkeep, frontend/.gitkeep, ml/.gitkeep, hardware/.gitkeep, docs/.gitkeep</files>
  <action>
    1. Create root `.gitignore` ignoring Python build artifacts (`__pycache__/`, `*.py[cod]`, `.pytest_cache/`, `.venv/`, `venv/`), Node artifacts (`node_modules/`, `dist/`), OS files (`.DS_Store`, `Thumbs.db`), IDE files (`.vscode/`, `.idea/`), and local raw data caches (`data/raw/*.mat`, `*.cache`).
    2. Create root `README.md` detailing the ThermoCell-AI project vision, 10-second pulse diagnostic approach, data provenance tags (REAL, SYNTHETIC, PREDICTED), tech stack, repository layout, and setup instructions.
    3. Ensure standard directories exist: `backend/`, `frontend/`, `ml/`, `hardware/`, and `docs/`.
  </action>
  <verify>Verify directory presence and check `.gitignore` and `README.md` contents.</verify>
  <done>Repository structure matches ARCH-01 with complete `.gitignore` and descriptive `README.md`.</done>
</task>

<task type="code">
  <name>Task 2: Backend Dependencies Specification (ARCH-02)</name>
  <files>backend/requirements.txt</files>
  <action>
    Define Python dependencies in `backend/requirements.txt` matching STACK.md:
    - `fastapi>=0.110.0,<1.0.0`
    - `uvicorn[standard]>=0.28.0,<1.0.0`
    - `pydantic>=2.6.0,<3.0.0`
    - `numpy>=1.24.0,<2.0.0`
    - `pandas>=2.0.0,<3.0.0`
    - `scikit-learn>=1.3.0,<2.0.0`
    - `scipy>=1.11.0,<2.0.0`
    - `opencv-python-headless>=4.8.0,<5.0.0`
    - `pytest>=8.0.0,<9.0.0`
    - `httpx>=0.27.0,<1.0.0`
  </action>
  <verify>Inspect `backend/requirements.txt` to confirm version constraints and complete package coverage.</verify>
  <done>`backend/requirements.txt` defines all required backend libraries with compatible version bounds.</done>
</task>

<task type="code">
  <name>Task 3: Frontend Project Configuration and Manifests (ARCH-02)</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tailwind.config.js, frontend/postcss.config.js, frontend/index.html</files>
  <action>
    Configure the frontend project structure:
    1. `frontend/package.json`: Dependencies `react`, `react-dom`, `lucide-react`, `clsx`, `tailwind-merge`. Dev dependencies `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `typescript`, `vite`, `tailwindcss`, `postcss`, `autoprefixer`. Scripts `dev`, `build`, `preview`.
    2. `frontend/vite.config.ts`: Vite React plugin configuration.
    3. `frontend/tsconfig.json`: TypeScript compiler options for React JSX, strict mode, and ES2020 target.
    4. `frontend/tailwind.config.js` and `frontend/postcss.config.js`: Tailwind styling configuration scanning `src/**/*.{js,ts,jsx,tsx}`.
    5. `frontend/index.html`: Entry HTML document mounting the React root.
  </action>
  <verify>Inspect frontend configuration files to ensure valid JSON and TypeScript/Tailwind configurations.</verify>
  <done>Frontend configuration files established satisfying ARCH-02.</done>
</task>

## Verification

- Confirm directories `backend/`, `frontend/`, `ml/`, `hardware/`, and `docs/` exist.
- Confirm `.gitignore` and `README.md` are present at the repository root.
- Confirm `backend/requirements.txt` specifies all required packages.
- Confirm `frontend/package.json`, `vite.config.ts`, and `tailwind.config.js` are present and valid.

## Success Criteria

- [ ] Directory layout matches ARCH-01.
- [ ] Backend dependencies file `backend/requirements.txt` is created with proper version pins satisfying ARCH-02.
- [ ] Frontend manifests and build configurations are created satisfying ARCH-02.
